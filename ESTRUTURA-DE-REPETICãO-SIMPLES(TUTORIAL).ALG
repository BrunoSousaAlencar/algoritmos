algoritmo "semnome"
var
   cont, n, s : inteiro
inicio
      cont <- 1
      s <- 0
      enquanto (cont <= 5) faca
        escreva ("Digite o valor:")
           leia(n)
        s <- s + n

      fimenquanto
      escreval("A soma de todos os valores foi: ", s)

fimalgoritmo